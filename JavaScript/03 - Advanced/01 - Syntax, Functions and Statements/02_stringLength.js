function stringLength(first, second, third) {
    let totalLenght = first.length + second.length + third.length;
    console.log(totalLenght);
    console.log(Math.floor(totalLenght / 3));
}

stringLength('chocolate', 'ice cream', 'cake');