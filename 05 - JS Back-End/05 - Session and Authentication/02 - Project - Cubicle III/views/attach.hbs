<h1>Attach a new sticker</h1>
<div class="form">
    <h2>{{cube.name}}</h2>
    <img class="cube" src="{{cube.imageUrl}}">
    {{#if stickers}}
    <form action="/details/{{cube._id}}/attach" method="POST">
        <label for="sticker">Stickers</label>
        <select id="sticker" name="sticker">
            {{#each stickers}}
            <option value="{{_id}}">{{name}}</option>
            {{/each}}
        </select>
        <input type="submit" value="Attach">
    </form>
    {{else}}
    <h3 class="italic">This cube has all available stickers so far or there is no available at all...</h3>
    {{/if}}
    <a class="btn" href="/products/details/{{cube._id}}">Back</a>
</div>